# MySQL 连接凭证（敏感信息）
# 生产环境请替换为真实密码，上线阿里云 RDS 时更新 host/username/password
apiVersion: v1
kind: Secret
metadata:
  name: mymall-mysql
  namespace: mymall
type: Opaque
stringData:
  host: host.docker.internal
  username: homestead
  password: secret
