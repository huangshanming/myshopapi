# 过渡期：指向同一套 mymall-api Pod，供 APISIX 路由 catalog 相关接口
apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: mymall
spec:
  selector:
    app: mymall-api
  ports:
    - port: 8888
      targetPort: 8888
