apiVersion: v1
kind: ConfigMap
metadata:
  name: order-service-config
  namespace: mymall
data:
  config.yaml: |
    server:
      http_port: 8888
      mode: release
    mysql:
      host: host.docker.internal
      port: 3306
      username: homestead
      password: secret
      charset: utf8mb4
      dbname: mymall
      max_open_conns: 20
      max_idle_conns: 5
    grpc:
      user_service: user-service:9090
      catalog_service: catalog-service:9090
    rabbitmq:
      host: host.docker.internal
      port: 5672
      username: mymall
      password: mymall
      vhost: /
      exchange: mymall.events
    telemetry:
      enabled: false
      service: order-service
